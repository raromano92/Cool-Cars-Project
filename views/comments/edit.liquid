{% layout "layout.liquid" %} {% block content %}
<div>
  {% for comment in car.Comments %}
  {% if comment._id == commentId %}
    <form class="edit-combox" action="/cars/{{ car._id }}/{{ comment._id }}?_method=PUT" method="POST">
    <fieldset>
      <legend>Edit Your Comment</legend>
      
      <input type="text" id="edit-com-entry" name="Comments" placeholder="Hmm..." value="">
      </input>

      </fieldset>
      
      <input type="submit" id="edit-btn" value="Update" />
    
</div>
</form>
{% endif %}
{% endfor %}
{% endblock %}